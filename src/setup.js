;(function(context) {

    'use strict';

    Module.setup = {
        namespace : 'MONKEY'
    };

    context[Module.setup.namespace] = {
        Components : {},
        Ajax       : {}
    };

})( window );
